// Funciones puras o pure functions
// Retornan la misma cantidad de entradas
// No modifican una variable global

const duplicar = numero => numero * 2;

const numero1 = 20;
const resultado = duplicar(numero1);